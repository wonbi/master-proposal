# 상품 제안서 — 사용 설명서

이 저장소는 **두 개의 화면**으로 이루어진 상품 제안서입니다.

| 화면 | 무엇 | 누가 보나 |
|------|------|-----------|
| 🌐 **공개 사이트** | 예쁜 상품 제안서 (인포그래픽) | 거래처(B2B 업체) — 링크만 있으면 열람 |
| 📝 **구글 시트** | 상품·가격·택배비를 수정하는 곳 | 나(관리자)만 |

**구글 시트에서 상품을 수정하면 → 공개 사이트에 자동으로 반영됩니다.** (거래처는 시트를 볼 수 없고, 완성된 제안서 화면만 봅니다.)

> 지금 당장은 시트를 연결하지 않아도 **기본 상품 24종**이 사이트에 그대로 표시됩니다.
> 아래 3단계를 하면 "내가 웹에서 수정 → 자동 반영" 구조가 완성됩니다.

---

## 1단계. 공개 사이트 켜기 (GitHub Pages)

한 번만 설정하면 됩니다.

1. GitHub에서 이 저장소(`wonbi/master-proposal`) 페이지로 갑니다.
2. 상단 **Settings**(설정) → 왼쪽 메뉴 **Pages** 클릭.
3. **Build and deployment** → **Source** 를 **GitHub Actions** 로 선택.
4. 끝입니다. 잠시 후(1~2분) 아래 두 곳 중 하나에서 사이트 주소가 나옵니다.
   - Settings → Pages 상단의 “Your site is live at …”
   - 저장소 상단 **Actions** 탭 → “Deploy to GitHub Pages” 실행 결과의 링크

주소는 보통 이렇게 생겼습니다:
```
https://wonbi.github.io/master-proposal/
```
이 주소를 거래처에 카톡·문자·메일로 보내면 됩니다. (로그인 없이 열립니다.)

> 앞으로는 이 저장소에 변경사항이 올라갈 때마다 **자동으로 다시 배포**됩니다.

---

## 2단계. 구글 시트 만들기 (내가 수정하는 곳)

1. [sheets.new](https://sheets.new) 로 새 구글 시트를 만듭니다.
2. 이 저장소의 **`상품시트-템플릿.csv`** 파일을 내려받습니다.
   (GitHub에서 파일 클릭 → **Download raw file**)
3. 구글 시트에서 **파일 → 가져오기 → 업로드** → 방금 받은 CSV 선택
   → “가져오기 위치: **현재 시트 바꾸기**” → **데이터 가져오기**.
4. 이제 상품 24종이 표로 들어옵니다. **첫 줄(제목 줄)은 절대 지우지 마세요.**

### 시트 공유 설정 (중요)
공개 사이트가 시트를 읽을 수 있어야 합니다.

- 오른쪽 위 **공유** 버튼 → **일반 액세스** 를
  **“링크가 있는 모든 사용자”** + 역할 **“뷰어”** 로 변경 → 완료.

> 걱정 마세요 — 시트 “주소”를 아는 사람만 열 수 있고, 그 주소는 사이트 코드 안에만 들어갑니다.
> 거래처에게는 시트가 아니라 완성된 제안서 화면만 보입니다.

---

## 3단계. 시트와 사이트 연결하기

1. 구글 시트 주소창을 봅니다. 이렇게 생겼습니다:
   ```
   https://docs.google.com/spreadsheets/d/1AbCdE...여기가_시트ID.../edit#gid=0
   ```
2. `/d/` 와 `/edit` 사이의 긴 문자열이 **시트 ID** 입니다. 복사합니다.
3. 이 저장소의 **`config.js`** 파일을 엽니다 (GitHub에서 파일 → 연필✏️ 아이콘으로 편집).
4. 맨 위 `sheetId: ""` 의 따옴표 안에 복사한 ID를 붙여넣습니다:
   ```js
   sheetId: "1AbCdE...여기가_시트ID...",
   sheetGid: "0",
   ```
   (주소 맨 뒤 `gid=` 뒤의 숫자를 `sheetGid` 에 넣으면 됩니다. 첫 시트는 보통 `0`.)
5. **Commit changes**(저장)를 누르면 → 사이트가 자동 재배포되고, 이제부터 **시트 = 공개 사이트**로 연결됩니다.

---

## 매일 쓰는 법 — 상품 수정하기

이제부터는 **구글 시트만** 고치면 됩니다. (코드 만질 일 없음)

- **가격 바꾸기**: `공급가` 칸 숫자만 바꾸면 됩니다. (예: `11500`)
- **택배비 / 택배사 바꾸기**: 해당 칸 수정.
- **상품 숨기기**: 맨 끝 `노출` 칸을 `숨김` 으로. (줄을 지우지 않아도 사이트에서 사라짐)
- **상품 추가**: 맨 아래 새 줄에 값을 채우면 됩니다.
- 수정 후 공개 사이트를 **새로고침**하면 반영됩니다. (몇 분 정도 늦게 보일 수 있어요 — 구글 캐시)

### 각 칸(열) 설명
| 열 | 뜻 | 예시 |
|----|-----|------|
| 카테고리 | `수산물` / `간편식품` / `생활용품` 중 하나 | 수산물 |
| 상품명 | 상품 이름 | 대왕 선동 활 쭈꾸미 1kg |
| 창고 | 사진 위 배지에 표시 | 동해 |
| 설명 | 상품명 아래 회색 작은 글씨 | 동해 창고 · 냉동 |
| 공급가 | 숫자만 (원). 콤마 없어도 됨 | 11500 |
| 택배사 | 택배 회사 | 씨제이대한통운 |
| 택배비 | 숫자만 (원) | 4000 |
| 면과세 | `면세` 또는 `과세` | 면세 |
| 사진 | 사진 파일명 또는 링크 (아래 참고) | fish-1.webp |
| 노출 | `표시` 또는 `숨김` | 표시 |

### 사진 넣는 법 (3가지 방법)
`사진` 칸에는 아래 중 아무거나 넣으면 됩니다.

1. **기본 제공 사진** — 이미 저장소에 있는 24장. 파일명만 적으면 됩니다.
   예: `fish-1.webp` (목록은 `images/products/` 폴더 참고)
2. **인터넷 이미지 주소** — `https://...jpg` 처럼 사진 URL 전체를 붙여넣기.
3. **구글 드라이브 사진** — 드라이브에서 사진 우클릭 → **링크 복사**
   (그 사진도 “링크가 있는 모든 사용자 - 뷰어”로 공유돼 있어야 함). 링크를 그대로 붙여넣으면 됩니다.

> 새 상품 사진을 저장소에 직접 올리고 싶으면, `images/products/` 폴더에 올린 뒤
> 파일명을 `사진` 칸에 적으면 됩니다.

---

## 회사·담당자 정보 바꾸기
`config.js` 파일 안에서 전화번호·이메일·카카오 ID·표지 문구 등을 바꿀 수 있습니다.
현재 값: 전화 `010-2326-5911` · 이메일 `feellost@naver.com` · 카카오 `wonbi123`.

---

## 잘 안 될 때
- **사이트에 옛날 내용이 보여요** → 구글 시트가 캐시돼서 그렇습니다. 몇 분 뒤 새로고침(Ctrl+F5).
- **시트를 연결했는데 기본 상품만 나와요** → ① 시트 공유가 “링크가 있는 모든 사용자 - 뷰어”인지, ② `config.js`의 `sheetId`가 정확한지, ③ 첫 줄 제목이 그대로인지 확인하세요.
- **사진이 안 보여요** → 파일명 철자 확인, 또는 드라이브 사진의 공유 설정 확인.
- 그래도 안 되면 이 저장소에서 저(Claude)에게 다시 요청하세요.
