-- =====================================================================
-- 상품 원본 링크(masterc.kr 등) 컬럼 추가 — Supabase SQL Editor 에 붙여넣고 RUN. (한 번만)
-- 이 링크가 있으면:
--  · 공개 사이트 상품명이 그 주소로 클릭되는 링크가 됩니다
--  · photo-updater.html 북마클릿이 이 주소에서 사진·스펙을 긁어와 자동으로 채워줍니다
-- =====================================================================

alter table public.products add column if not exists link text default '';
